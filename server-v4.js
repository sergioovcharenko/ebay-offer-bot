import http from 'node:http';
import {readFileSync,writeFileSync,existsSync} from 'node:fs';
import {randomUUID} from 'node:crypto';
const PORT=Number(process.env.PORT||3000), PROVIDER=process.env.EBAY_PROVIDER||'fake';
const DRY_RUN=(process.env.GLOBAL_DRY_RUN??'true')!=='false', DATA=process.env.DATA_FILE||'./data.json';
const CLIENT=process.env.EBAY_CLIENT_ID||'', SECRET=process.env.EBAY_CLIENT_SECRET||'', EBAY_RUNAME=process.env.EBAY_RUNAME||'';
const SCOPES=['https://api.ebay.com/oauth/api_scope','https://api.ebay.com/oauth/api_scope/commerce.identity.readonly'];
function fresh(){return {rules:[],offers:[],history:[],seen:[],settings:{notifyEnabled:true,notifyServer:'https://ntfy.sh',notifyTopic:'ebay-offer-'+randomUUID().replaceAll('-','')},ebayAccount:null,oauthState:null}}
function loadDb(){if(existsSync(DATA))try{return {...fresh(),...JSON.parse(readFileSync(DATA,'utf8'))}}catch{}return fresh()}
let db=loadDb(), appToken={token:null,expires:0}; const save=()=>writeFileSync(DATA,JSON.stringify(db,null,2));
const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot',"'":'&#39;'}[c]));
const txt=s=>String(s||'').normalize('NFKC').toLowerCase().replace(/[×xX]/g,'x').replace(/\s+/g,' ').trim();
const round=n=>Math.round((Number(n)+Number.EPSILON)*100)/100;
function json(res,status,data){res.writeHead(status,{'content-type':'application/json; charset=utf-8','cache-control':'no-store'});res.end(JSON.stringify(data))}
function html(res,status,content){res.writeHead(status,{'content-type':'text/html; charset=utf-8','cache-control':'no-store'});res.end(content)}
async function body(req){let s='';for await(const c of req){s+=c;if(s.length>1e6)throw Error('Body too large')}return s?JSON.parse(s):{}}
function validateRule(x){const k=String(x.searchKeywords||'').trim();if(!k)throw Error('Ключові слова обов’язкові');const mode=x.offerMode==='percentage'?'percentage':'fixed';const ml=+x.maxListingPrice,mo=+x.maxOfferAmount,step=+(x.offerStepAmount||1),v=mode==='percentage'?+x.discountPercent:+x.fixedOfferAmount;if(!(ml>0&&mo>0&&step>0&&v>0))throw Error('Ціни та крок мають бути більше 0');if(mode==='percentage'&&v>=100)throw Error('Знижка має бути 1–99%');return {id:randomUUID(),name:k,productName:k,searchKeywords:k,sizeVariant:String(x.sizeVariant||'').trim(),condition:x.condition||'ANY',maxListingPrice:ml,requireBestOffer:true,offerMode:mode,discountPercent:mode==='percentage'?v:undefined,fixedOfferAmount:mode==='fixed'?v:undefined,offerStepAmount:step,maxOfferAmount:mo,enabled:true,createdAt:new Date().toISOString()}}
async function appAccessToken(){if(appToken.token&&Date.now()<appToken.expires)return appToken.token;if(!CLIENT||!SECRET)throw Error('eBay credentials are not configured');const basic=Buffer.from(`${CLIENT}:${SECRET}`).toString('base64');const r=await fetch('https://api.ebay.com/identity/v1/oauth2/token',{method:'POST',headers:{Authorization:`Basic ${basic}`,'Content-Type':'application/x-www-form-urlencoded'},body:'grant_type=client_credentials&scope='+encodeURIComponent('https://api.ebay.com/oauth/api_scope')});if(!r.ok)throw Error('eBay auth failed '+r.status);const j=await r.json();appToken={token:j.access_token,expires:Date.now()+(j.expires_in-60)*1000};return j.access_token}
async function exchangeCode(code){const basic=Buffer.from(`${CLIENT}:${SECRET}`).toString('base64');const form=new URLSearchParams({grant_type:'authorization_code',code,redirect_uri:EBAY_RUNAME});const r=await fetch('https://api.ebay.com/identity/v1/oauth2/token',{method:'POST',headers:{Authorization:`Basic ${basic}`,'Content-Type':'application/x-www-form-urlencoded'},body:form});if(!r.ok)throw Error('eBay user auth failed '+r.status);return r.json()}
async function refreshUserToken(){const a=db.ebayAccount;if(!a?.refreshToken)throw Error('eBay account not connected');if(a.accessToken&&Date.now()<a.accessTokenExpiresAt-60000)return a.accessToken;const basic=Buffer.from(`${CLIENT}:${SECRET}`).toString('base64');const form=new URLSearchParams({grant_type:'refresh_token',refresh_token:a.refreshToken,scope:SCOPES.join(' ')});const r=await fetch('https://api.ebay.com/identity/v1/oauth2/token',{method:'POST',headers:{Authorization:`Basic ${basic}`,'Content-Type':'application/x-www-form-urlencoded'},body:form});if(!r.ok)throw Error('eBay refresh failed '+r.status);const j=await r.json();a.accessToken=j.access_token;a.accessTokenExpiresAt=Date.now()+j.expires_in*1000;save();return a.accessToken}
async function identity(token){let r=await fetch('https://apiz.ebay.com/commerce/identity/v1/user/',{headers:{Authorization:`Bearer ${token}`}});if(r.status===405)r=await fetch('https://apiz.ebay.com/commerce/identity/v1/user/',{method:'POST',headers:{Authorization:`Bearer ${token}`}});if(!r.ok)throw Error('Не вдалося отримати eBay профіль: '+r.status);return r.json()}
function normalize(i){const c=txt(i.condition);let condition='UNKNOWN';if(c.includes('new with tags'))condition='NEW_WITH_TAGS';else if(c.includes('new without tags'))condition='NEW_WITHOUT_TAGS';else if(c.includes('open box'))condition='OPEN_BOX';else if(c==='new'||c.startsWith('new '))condition='NEW';else if(c.includes('refurb'))condition='REFURBISHED';else if(c.includes('used')||c.includes('pre-owned'))condition='USED';return {itemId:i.itemId,title:i.title||'',price:+(i.price?.value||0),currency:i.price?.currency||'USD',condition,buyingOptions:i.buyingOptions||[],itemUrl:i.itemWebUrl||'',imageUrl:i.image?.imageUrl||''}}
function matches(r,l){const t=txt(l.title);for(const w of txt(r.searchKeywords).split(' ').filter(Boolean))if(!t.includes(w))return false;if(r.sizeVariant&&!t.includes(txt(r.sizeVariant)))return false;if(r.condition!=='ANY'&&l.condition!==r.condition)return false;if(r.requireBestOffer&&!l.buyingOptions.includes('BEST_OFFER'))return false;return l.price<=r.maxListingPrice}
async function search(rule){if(PROVIDER==='fake')return[];const token=await appAccessToken(),q=encodeURIComponent(`${rule.searchKeywords} ${rule.sizeVariant||''}`.trim()),filter=encodeURIComponent(`price:[0..${rule.maxListingPrice}],priceCurrency:USD`);const r=await fetch(`https://api.ebay.com/buy/browse/v1/item_summary/search?q=${q}&limit=50&filter=${filter}`,{headers:{Authorization:`Bearer ${token}`,'X-EBAY-C-MARKETPLACE-ID':'EBAY_US'}});if(!r.ok)throw Error('eBay search failed '+r.status);const j=await r.json();return (j.itemSummaries||[]).map(normalize)}
async function runRule(id){const rule=db.rules.find(x=>x.id===id);if(!rule)throw Error('Rule not found');const items=await search(rule);let prepared=0;for(const l of items){const key=rule.id+'|'+l.itemId;if(db.seen.includes(key)||!matches(rule,l))continue;const amount=round(Math.min(rule.offerMode==='percentage'?l.price*(1-rule.discountPercent/100):rule.fixedOfferAmount,rule.maxOfferAmount));db.offers.unshift({id:randomUUID(),ruleId:rule.id,itemId:l.itemId,title:l.title,itemUrl:l.itemUrl,imageUrl:l.imageUrl,listingPrice:l.price,currency:l.currency,amount,status:'OFFER_READY',attempt:1,createdAt:new Date().toISOString()});db.seen.push(key);prepared++}save();return {fetched:items.length,offersPrepared:prepared}}
const CSS=`:root{--bg:#f2f2f7;--card:#ffffffdc;--text:#111;--muted:#6e6e73;--line:#3c3c4324;--blue:#007aff;--soft:#eaf3ff;--red:#ff3b30}html[data-theme=dark]{--bg:#000;--card:#1c1c1ee8;--text:#f5f5f7;--muted:#98989d;--line:#ffffff1f;--blue:#0a84ff;--soft:#17263a;--red:#ff453a}*{box-sizing:border-box}body{margin:0;background:var(--bg);color:var(--text);font-family:-apple-system,BlinkMacSystemFont,system-ui}header{position:sticky;top:0;display:flex;justify-content:space-between;padding:16px 20px;background:var(--card);backdrop-filter:blur(20px);border-bottom:1px solid var(--line)}main{max-width:980px;margin:auto;padding:16px}.card{background:var(--card);border:1px solid var(--line);border-radius:22px;padding:18px;margin:14px 0}.grid{display:grid;grid-template-columns:repeat(2,1fr);gap:12px}.field{display:flex;flex-direction:column;gap:6px}label{font-size:13px;font-weight:700}.muted{color:var(--muted);font-size:13px}input,select,button{width:100%;font:inherit;border-radius:14px;padding:13px}input,select{background:var(--card);color:var(--text);border:1px solid var(--line)}button{border:0;background:var(--blue);color:white;font-weight:700}.secondary{background:var(--soft);color:var(--blue)}.danger{background:#ff3b3020;color:var(--red)}.row{display:flex;gap:8px}.row>*{flex:1}.pill{display:inline-block;padding:5px 9px;border-radius:999px;background:var(--soft);color:var(--blue);font-size:12px;font-weight:700}.item{border-top:1px solid var(--line);padding-top:14px;margin-top:14px}.top{display:flex;justify-content:space-between;gap:10px}.icon{width:44px;padding:8px}.legal{max-width:760px;margin:40px auto;padding:0 16px}.legal h1{font-size:34px}.legal p,.legal li{line-height:1.55}@media(max-width:650px){.grid{grid-template-columns:1fr}.row{flex-direction:column}header{position:static}}`;
const HTML=`<!doctype html><html lang="uk"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>eBay Offer Bot</title><style>${CSS}</style></head><body><header><div><b>eBay Offer Bot</b><div class="muted">Пошук + Best Offer</div></div><button class="icon" onclick="theme()">◐</button></header><main><div class="card"><h3>Підключений eBay акаунт</h3><div id="account"></div></div><div class="card"><h3>Нове правило</h3><div class="grid"><div class="field"><label>Ключові слова</label><input id="keywords" placeholder="Merrell Moab 3 Mid GTX"></div><div class="field"><label>Товар / модель</label><input id="product" readonly></div><div class="field"><label>Розмір / варіант</label><input id="size"></div><div class="field"><label>Стан</label><select id="condition"><option value="ANY">Будь-який</option><option value="NEW">Новий</option><option value="USED">Вживаний</option></select></div><div class="field"><label>Макс. ціна, USD</label><input id="maxListing" type="number"></div><div class="field"><label>Тип пропозиції</label><select id="mode"><option value="fixed">Фіксована</option><option value="percentage">% знижки</option></select></div><div class="field"><label>Початкова пропозиція</label><input id="offerValue" type="number"></div><div class="field"><label>Крок після відхилення</label><input id="step" type="number"></div><div class="field"><label>Максимум пропозиції</label><input id="maxOffer" type="number"></div></div><p><button onclick="addRule()">Додати правило</button></p></div><div class="card"><h3>Сповіщення</h3><div id="notify"></div></div><div class="card"><h3>Правила</h3><div id="rules"></div></div><div class="card"><h3>Пропозиції</h3><div id="offers"></div></div></main><script>
const $=id=>document.getElementById(id);const E=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));async function api(p,o){const r=await fetch(p,{headers:{'content-type':'application/json'},...o});const j=await r.json();if(!r.ok)throw Error(j.error||'Error');return j}function theme(){const d=document.documentElement;const n=d.dataset.theme==='dark'?'light':'dark';d.dataset.theme=n;localStorage.theme=n}document.documentElement.dataset.theme=localStorage.theme||'light';$('keywords').oninput=()=>$('product').value=$('keywords').value.trim();function reset(){for(const x of ['keywords','product','size','maxListing','offerValue','step','maxOffer'])$(x).value='';$('condition').value='ANY';$('mode').value='fixed'}async function addRule(){try{const mode=$('mode').value,v=+$('offerValue').value;await api('/api/rules',{method:'POST',body:JSON.stringify({searchKeywords:$('keywords').value,sizeVariant:$('size').value,condition:$('condition').value,maxListingPrice:+$('maxListing').value,offerMode:mode,discountPercent:mode==='percentage'?v:undefined,fixedOfferAmount:mode==='fixed'?v:undefined,offerStepAmount:+$('step').value,maxOfferAmount:+$('maxOffer').value})});reset();load()}catch(e){alert(e.message)}}async function del(id){if(confirm('Видалити правило?')){await api('/api/rules/'+id,{method:'DELETE'});load()}}async function run(id){try{const x=await api('/api/rules/'+id+'/run',{method:'POST'});alert('Знайдено: '+x.fetched+'; пропозицій: '+x.offersPrepared);load()}catch(e){alert(e.message)}}async function logout(){await api('/api/ebay/account',{method:'DELETE'});load()}async function load(){const [rules,offers,s]=await Promise.all([api('/api/rules'),api('/api/offers'),api('/api/settings')]);$('account').innerHTML=s.ebayAccount?.connected?'<div class="top"><div><span class="pill">Підключено</span><p><b>'+E(s.ebayAccount.username||s.ebayAccount.userId||'eBay акаунт')+'</b><br><span class="muted">'+E(s.ebayAccount.marketplace||'')+'</span></p></div></div><div class="row"><a href="/auth/ebay/login"><button>Змінити акаунт</button></a><button class="danger" onclick="logout()">Вийти</button></div>':'<p class="muted">Підключи саме той основний акаунт, від імені якого мають виконуватися дозволені eBay дії.</p><a href="/auth/ebay/login"><button>Увійти через eBay</button></a>';$('notify').innerHTML='<p class="muted">Канал ntfy: <b>'+E(s.notifications.notifyTopic)+'</b></p><a target="_blank" href="'+E(s.notifications.notifyServer+'/'+s.notifications.notifyTopic)+'"><button>Підключити сповіщення</button></a>';$('rules').innerHTML=rules.length?rules.map(r=>'<div class="item"><div class="top"><b>'+E(r.name)+'</b><span class="pill">активне</span></div><p class="muted">'+E(r.sizeVariant||'')+' · до '+E(r.maxListingPrice)+' USD</p><div class="row"><button onclick="run(\\''+r.id+'\\')">Перевірити зараз</button><button class="danger" onclick="del(\\''+r.id+'\\')">Видалити</button></div></div>').join(''):'<p class="muted">Правил немає.</p>';$('offers').innerHTML=offers.length?offers.map(o=>'<div class="item"><b>'+E(o.title)+'</b><p>Ціна: '+E(o.listingPrice)+' '+E(o.currency)+'<br><b>Наша пропозиція: '+E(o.amount)+' '+E(o.currency)+'</b></p><a target="_blank" href="'+E(o.itemUrl)+'"><button class="secondary">Відкрити товар на eBay</button></a></div>').join(''):'<p class="muted">Пропозицій немає.</p>'}load();</script></body></html>`;
const PRIVACY=`<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Privacy Policy — eBay Offer Bot</title><style>${CSS}</style></head><body><div class="legal"><h1>Privacy Policy</h1><p><b>eBay Offer Bot</b> is a private tool for searching eBay listings and managing offer workflows for the account owner.</p><h2>Data used</h2><ul><li>eBay account identifiers needed to show which account is connected.</li><li>OAuth access and refresh tokens needed to keep the authorized eBay account connected.</li><li>Listing identifiers, listing details, search rules, prices and offer workflow data needed for the service to operate.</li></ul><h2>How data is used</h2><p>Data is used only to provide the requested eBay search, account connection and offer-workflow features. The application does not sell personal data or use it for advertising.</p><h2>Sharing</h2><p>Data is sent only to service providers required to operate the application, including eBay APIs and the hosting/notification services configured by the account owner.</p><h2>Retention and control</h2><p>The connected eBay account can be disconnected from the application at any time. Search rules can also be deleted in the application.</p><h2>Contact</h2><p>For privacy questions, use the contact details associated with the eBay Developer account that operates this application.</p><p><a href="/">Return to eBay Offer Bot</a></p></div></body></html>`;
const DECLINED=`<!doctype html><html lang="uk"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>eBay authorization cancelled</title><style>${CSS}</style></head><body><div class="legal"><h1>Авторизацію eBay скасовано</h1><p>Акаунт не був підключений. Можеш повернутися в eBay Offer Bot і повторити вхід, коли буде зручно.</p><p><a href="/"><button>Повернутися до бота</button></a></p></div></body></html>`;
const server=http.createServer(async(req,res)=>{try{const u=new URL(req.url,'http://local'),p=u.pathname;if(p==='/'&&req.method==='GET')return html(res,200,HTML);if(p==='/privacy'&&req.method==='GET')return html(res,200,PRIVACY);if(p==='/auth/ebay/declined'&&req.method==='GET'){db.oauthState=null;save();return html(res,200,DECLINED)}if((p==='/health'||p==='/api/health')&&req.method==='GET')return json(res,200,{ok:true,provider:PROVIDER,dryRun:DRY_RUN});if(p==='/auth/ebay/login'){if(!EBAY_RUNAME)return json(res,400,{error:'EBAY_RUNAME не налаштований'});db.oauthState=randomUUID();save();const q=new URLSearchParams({client_id:CLIENT,response_type:'code',redirect_uri:EBAY_RUNAME,scope:SCOPES.join(' '),state:db.oauthState,prompt:'login'});res.writeHead(302,{Location:'https://auth.ebay.com/oauth2/authorize?'+q});return res.end()}if(p==='/auth/ebay/callback'){if(!u.searchParams.get('code'))throw Error('eBay не повернув authorization code');if(!db.oauthState||u.searchParams.get('state')!==db.oauthState)throw Error('Невірний OAuth state');const t=await exchangeCode(u.searchParams.get('code'));const profile=await identity(t.access_token);db.ebayAccount={connected:true,userId:profile.userId||'',username:profile.username||'',marketplace:profile.registrationMarketplaceId||'',accountType:profile.accountType||'',accessToken:t.access_token,accessTokenExpiresAt:Date.now()+t.expires_in*1000,refreshToken:t.refresh_token||'',refreshTokenExpiresIn:t.refresh_token_expires_in||0,connectedAt:new Date().toISOString()};db.oauthState=null;save();res.writeHead(302,{Location:'/?ebay=connected'});return res.end()}if(p==='/api/settings')return json(res,200,{provider:PROVIDER,dryRun:DRY_RUN,ebayConfigured:Boolean(CLIENT&&SECRET),oauthConfigured:Boolean(EBAY_RUNAME),ebayAccount:db.ebayAccount?{connected:true,userId:db.ebayAccount.userId,username:db.ebayAccount.username,marketplace:db.ebayAccount.marketplace,accountType:db.ebayAccount.accountType}:null,notifications:db.settings});if(p==='/api/ebay/account'&&req.method==='DELETE'){db.ebayAccount=null;save();return json(res,200,{ok:true})}if(p==='/api/rules'&&req.method==='GET')return json(res,200,db.rules);if(p==='/api/rules'&&req.method==='POST'){const r=validateRule(await body(req));db.rules.unshift(r);save();return json(res,201,r)}let m=p.match(/^\/api\/rules\/([^/]+)$/);if(m&&req.method==='DELETE'){db.rules=db.rules.filter(x=>x.id!==m[1]);db.offers=db.offers.filter(x=>x.ruleId!==m[1]);db.seen=db.seen.filter(x=>!x.startsWith(m[1]+'|'));save();return json(res,200,{ok:true})}m=p.match(/^\/api\/rules\/([^/]+)\/run$/);if(m&&req.method==='POST')return json(res,200,await runRule(m[1]));if(p==='/api/offers')return json(res,200,db.offers);return json(res,404,{error:'Not found'})}catch(e){return json(res,400,{error:e.message})}});server.listen(PORT,()=>console.log(`eBay Offer Bot v4 on ${PORT}; provider=${PROVIDER}; dryRun=${DRY_RUN}`));
